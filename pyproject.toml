[project]
name = "asyncio-book"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "EvP", email = "cwv6nx9ny8@privaterelay.appleid.com" }
]
requires-python = ">=3.13"
dependencies = [
    "requests>=2.34.2",
]

[project.scripts]
asyncio-book = "asyncio_book:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
